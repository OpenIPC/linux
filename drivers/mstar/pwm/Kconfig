config MS_PWM

tristate "MSTAR_PWM"

        help
          Say Y here to enable the driver for the PWM.

          If unsure, say Y.

          To compile this driver as a module, choose M here: the
          module will be called mdrv_ts.

select PWM

help
    MStar PWM driver function

	

